<Window x:Class="RVUCounter.Views.DropboxCodeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dropbox Authorization"
        Height="250" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Dropbox Authorization" FontWeight="Bold" FontSize="14"
                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,15">
            A browser window has opened. Sign in to Dropbox and click "Allow" to authorize RVU Counter.
            Then paste the authorization code below:
        </TextBlock>

        <TextBox Grid.Row="2" x:Name="CodeTextBox" Height="30" FontSize="13"
                 VerticalContentAlignment="Center" Padding="5,0"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel" Width="80" Padding="5" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="OK" Width="80" Padding="5" Click="OK_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
